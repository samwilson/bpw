{
    "require": {
        "samwilson/basildon": "^8"
    },
    "scripts": {
        "build": [
            "basildon build ."
        ],
        "deploy": [
            "@build",
            "./node_modules/.bin/netlify deploy --prod"
        ]
    }
}
