{% autoescape 'tex' %}
\documentclass[a4paper]{book}

\usepackage{ graphicx, url, listings, float, booktabs, longtable, array, lscape }

\usepackage{enotez}
\setenotez{list-name=References, backref=true, totoc=chapter}
\let\footnote=\endnote

\usepackage{hyperref}
\hypersetup{
    colorlinks = true,  % Colours links instead of ugly boxes
    urlcolor   = black, % Colour for external hyperlinks
    linkcolor  = black, % Colour of internal links
    citecolor  = black  % Colour of citations
}

\setcounter{secnumdepth}{0}
\setlength{\parindent}{0mm}
\setlength{\parskip}{2mm}

\lstset{
    basicstyle=\normalsize\fontencoding{T1}\ttfamily,
    showstringspaces=true
}

\title{ {{site.config.title}} }
\author{ \url{ {{site.config.url}} } }

\begin{document}
    \maketitle
    \tableofcontents

    {{ page.body|md2latex|raw }}

    {% for p in database.query( 'SELECT * FROM pages WHERE id LIKE "/about/%" ORDER BY id ASC' ).fetchAll() %}

        \chapter{ {{ p.title }} }

        {{ p.body|md2latex|raw }}

    {% endfor %}

\printendnotes

\end{document}
{% endautoescape %}
