{% set p = database.query('SELECT * FROM pages WHERE id = "' ~ shortcode.attrs.1 ~ '"').fetch() %}{#

#}\textsc{{'{'}}{% if shortcode.attrs.2 is defined %}{{shortcode.attrs.2}}{% else %}{{p.title}}{% endif %}{{'}'}}\footnote{{'{'}}\url{{'{'}}{{site.config.url ~ p.id}}.html{{'}'}}{{'}'}}
